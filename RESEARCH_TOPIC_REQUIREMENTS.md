# 课题功能需求说明

## 1. 功能定位

“课题”不是普通笔记，也不是普通待办事项。它更像一个个人研究/项目推进工作台，用来整理一个长期目标背后的问题、资料、任务、依赖关系和阶段判断。

它要解决的问题是：

- 一个复杂目标往往不是一条笔记能说清楚，而是一组互相关联的节点。
- 用户在研究、开发、写论文、学习技术时，会不断遇到资料、概念、前置条件、任务、结论。
- 这些内容需要能沉淀、能关联、能重新整理，而不是只按时间顺序堆在一起。

核心目标：

> 以一个课题为中心，把零散想法、参考资料、前置知识、阶段任务、结论判断组织成可编辑、可视化、可长期维护的关系图谱。

## 2. 设计原则

- 不依赖在线 AI 服务，功能本身是本地记录和整理工具。
- AI 可以作为开发者或使用者的外部辅助，但软件本身不强制接入 AI。
- 保持旧安卓项目的兼容性，尽量少引入大版本依赖。
- 操作要顺手：新增、编辑、关联、拖动、保存都要轻量。
- 内容优先，界面工具栏不能占太多视图空间。
- 数据要能长期保存，不能只是一次运行内有效。

## 3. 核心概念

### 3.1 课题

一个课题代表一个大的研究方向、项目、论文、产品或学习目标。

例如：

- 如何写一篇论文
- 研究 FreeCAD / CAD 软件开发路线
- 软体机器人避障课题
- 开发一个安卓工具软件

### 3.2 节点

课题下面由多个节点组成。每个节点代表一个具体信息单元。

节点可以是：

- 问题：要解决什么，为什么值得做。
- 概念：需要理解的知识点。
- 资料：网页、视频、论文、链接。
- 任务：需要完成的动作。
- 工具：框架、软件、库、平台。
- 结论：阶段判断、复盘、最终沉淀。
- 灵感：临时想法、还未分类的内容。

### 3.3 关联关系

节点之间可以有关联。关联的含义比较宽，可以表示：

- 前置条件
- 依赖关系
- 参考来源
- 推导关系
- 下一步任务

例如：

```text
确定 Motivation -> 实验方法设计 -> 整理实验结果图 -> 打磨 Abstract
```

在数据上，一个节点保存它关联的前置节点 ID。这样图谱可以根据关系画出连线。

## 4. 主要功能需求

### 4.1 新建课题

- 新建课题默认应为空白课题。
- 不应自动塞入大量示例数据，避免干扰用户真实使用。
- 示例数据可以保留在代码中，作为开发测试或以后模板功能的基础。

### 4.2 节点编辑

- 单击节点：编辑当前节点。
- 长按节点：在当前节点下面新增一个关联节点。
- 新增或编辑节点后应自动保存，减少忘记点外层保存按钮的风险。
- 节点编辑应使用弹窗，避免在列表里嵌套复杂输入导致滑动冲突。

节点编辑字段建议包括：

- 节点类型
- 标题
- 内容
- 资料链接
- 关联节点

### 4.3 图谱视图

课题需要有图谱视图，用来直观看到节点关系。

图谱视图要求：

- 节点用卡片/矩形展示。
- 不同类型节点使用不同颜色标识。
- 节点之间用曲线连接。
- 支持拖动画布。
- 支持双指缩放。
- 支持双击空白处逐级放大，放到最大后回到适合屏幕的缩放。
- 支持拖动节点位置。
- 拖动后的节点位置应保存。
- 支持重排，清除手动位置并恢复自动布局。
- 重排前必须二次确认，避免误操作清掉手动布局。

### 4.4 列表/卡片视图

除了图谱视图，也需要保留列表或卡片视图。

原因：

- 长内容阅读时，列表比图谱更清晰。
- 手机上图谱空间有限，卡片视图适合细读。
- 图谱适合看关系，卡片适合看内容。

视图切换要求：

- 顶部工具栏提供图谱/卡片切换。
- 用户上次选择的视图应保存。
- 下次打开同一个课题时，保持上次视图。

### 4.5 全屏查看

图谱需要支持全屏查看。

全屏要求：

- 尽量横屏展示，提升横向空间。
- 顶部不要放厚重标题栏。
- 只保留透明的关闭按钮。
- 全屏里不要放“重排”按钮，避免误触。
- 关闭全屏后回到原页面。

### 4.6 视口保持

图谱模式下，切换编辑/预览、保存、重新渲染时，不能突然回到初始位置。

要求：

- 记住当前缩放比例。
- 记住当前画布偏移。
- 重新渲染后恢复用户刚才看到的位置。

### 4.7 自动布局与手动布局

默认自动布局：

- 根据节点关联关系计算层级。
- 没有关联的节点从课题根节点连出。
- 有前置节点的节点放到前置节点的右侧层级。

手动布局：

- 用户拖动节点后，保存节点坐标。
- 有手动坐标的节点优先使用手动位置。
- 点“重排”后清除所有手动坐标，恢复自动布局。

## 5. 交互要求

编辑模式：

- 单击节点：编辑节点。
- 长按节点：新增关联节点。
- 拖动节点：调整节点位置。
- 拖动空白处：移动画布。
- 双指：缩放图谱。

预览模式：

- 单击节点：查看节点详情。
- 拖动空白处：移动画布。
- 双指：缩放图谱。
- 不应误改节点位置。

工具栏：

- 图谱/卡片切换。
- 新建节点。
- 全屏。
- 重排。
- 编辑/确定。

确定按钮：

- 对课题和待办事项，不应直接退出页面。
- 应保存当前内容并切换到预览模式。

## 6. 数据结构要求

课题数据建议使用 JSON 保存。

课题字段：

```text
dataType
title
problem
goal
conclusion
viewMode
nodes
```

节点字段：

```text
id
type
title
content
links
relatedNodeIds
mapPositioned
mapX
mapY
```

说明：

- `id` 用于节点之间建立稳定关系。
- `relatedNodeIds` 保存当前节点关联的前置节点。
- `mapPositioned/mapX/mapY` 保存手动拖动后的位置。
- `viewMode` 保存用户上次选择的是图谱还是卡片。
- “课题根节点”只是图谱视图里的视觉根框，不保存进 `nodes`。没有 `relatedNodeIds` 的节点默认从课题根框连出，这样 Android 端和桌面端的数据语义保持一致。

## 7. 示例一：如何写一篇论文

课题标题：

```text
如何写一篇论文
```

问题定义：

```text
把一个研究想法推进成可以投稿的论文。重点不是一上来就写 Abstract，而是先讲清楚 motivation、gap、方法和结果是否能支撑贡献。
```

可能节点：

```text
确定 Motivation
现有工作与研究缺口
实验方法与方案设计
阶段汇报与导师评估
画概念图 / Framework 图
整理实验结果图
建立 TeX 项目
论文结构
打磨 Introduction 和引用
反复打磨 Abstract
整体润色与一致性检查
投稿前检查
```

关系示例：

```text
确定 Motivation -> 现有工作与研究缺口
确定 Motivation -> 实验方法与方案设计
实验方法与方案设计 -> 阶段汇报与导师评估
实验方法与方案设计 -> 画概念图 / Framework 图
实验方法与方案设计 -> 整理实验结果图
画概念图 / Framework 图 -> 建立 TeX 项目
整理实验结果图 -> 建立 TeX 项目
建立 TeX 项目 -> 论文结构
现有工作与研究缺口 -> 打磨 Introduction 和引用
打磨 Introduction 和引用 -> 反复打磨 Abstract
整理实验结果图 -> 反复打磨 Abstract
论文结构 -> 整体润色与一致性检查
反复打磨 Abstract -> 整体润色与一致性检查
整体润色与一致性检查 -> 投稿前检查
```

这个例子体现的是：论文写作不是线性写作，而是一个不断验证问题、方法、结果和叙事的过程。

## 8. 示例二：研究 FreeCAD / CAD 软件开发

课题标题：

```text
研究 FreeCAD 与 CAD 软件开发路线
```

问题定义：

```text
如果想开发一个 CAD 软件，需要理解 CAD 的几何内核、建模流程、UI 框架、数据结构、渲染方式，以及开源项目 FreeCAD 的架构和依赖。
```

可能节点：

```text
CAD 软件目标定义
FreeCAD 项目结构
OpenCascade / OCC 几何内核
参数化建模
草图约束求解
三维视图与渲染
Qt 界面框架
Python 脚本扩展
文件格式与模型保存
ImGui 即时界面
计算机图形学基础
游戏引擎/渲染管线参考
最小 CAD Demo
```

关系示例：

```text
CAD 软件目标定义 -> FreeCAD 项目结构
FreeCAD 项目结构 -> OpenCascade / OCC 几何内核
OpenCascade / OCC 几何内核 -> 参数化建模
参数化建模 -> 草图约束求解
OpenCascade / OCC 几何内核 -> 三维视图与渲染
三维视图与渲染 -> 计算机图形学基础
计算机图形学基础 -> 游戏引擎/渲染管线参考
FreeCAD 项目结构 -> Qt 界面框架
Qt 界面框架 -> Python 脚本扩展
参数化建模 -> 文件格式与模型保存
ImGui 即时界面 -> 最小 CAD Demo
OpenCascade / OCC 几何内核 -> 最小 CAD Demo
三维视图与渲染 -> 最小 CAD Demo
```

这个例子体现的是：开发 CAD 不是只学一个库，而是要把几何内核、建模逻辑、界面、渲染、文件结构串成一个知识网络。

## 9. 给 AI 编程时的提示词基准

后续如果让 AI 继续开发这个功能，可以这样描述：

```text
我要在一个旧 Android Java 项目里维护“课题”功能。这个功能不是普通笔记，而是一个本地的课题研究/项目推进图谱。

一个课题包含多个节点，每个节点有类型、标题、内容、资料链接和关联节点。关联节点表示前置条件、依赖、参考或推导关系。图谱视图要用自定义 View 绘制节点和贝塞尔曲线连接线，支持拖动画布、双指缩放、双击空白处放大、拖动节点位置、保存节点坐标、重排自动布局、全屏横屏查看。列表/卡片视图用于阅读内容。用户上次选择的视图要保存。

不要引入大版本依赖，不要大改旧项目架构。优先保持 Android 11 和旧项目编译环境兼容。新增或编辑节点后自动保存。确定按钮只切换预览，不直接退出页面。
```

这个文档本身就是需求来源。实现可以调整，但不应偏离这里描述的产品意图。
